import { SwiperDirective } from './swiper.directive';

describe('SwiperDirective', () => {
  it('should create an instance', () => {
    const directive = SwiperDirective;
    expect(directive).toBeTruthy();
  });
});
